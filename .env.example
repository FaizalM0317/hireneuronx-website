# Copy this file to .env.local and fill in real values for local Vercel dev.
# For production, set these in the Vercel dashboard (Project → Settings → Environment Variables).
# NEVER commit real credentials. .env* files are git-ignored.

ADMIN_USERNAME=your-username
ADMIN_PASSWORD=a-long-random-password
